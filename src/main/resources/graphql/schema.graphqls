enum Gender {
    MALE, FEMALE
}

type User {
    id: ID!
    name: String!
    gender: Gender!
}

type Query {
    getUsers: [User]
}

type Mutation {
    createUser(name: String!, gender: Gender!): User
    updateUser(id: ID, name: String!, gender: Gender!): User
}